# 日期：2026-02-02

## 📌 今日目标

完成第一阶段的基础搭建，验证"数据驱动"的核心逻辑。

---

## 🎯 任务清单

### 1. 安装依赖

```bash
npm install antd zustand lucide-react lodash-es
```

**说明：**

- `antd`：UI 组件库
- `zustand`：轻量级状态管理
- `lucide-react`：图标库
- `lodash-es`：工具函数（用于深拷贝 Schema）

### 2. 定义 Schema 类型

- [ ] 创建 `src/types/schema.ts`
- [ ] 定义 `ComponentType` 枚举
- [ ] 定义 `FormItemSchema` 接口
- [ ] 定义 `PageSchema` 接口

### 3. 手写极简 Renderer

- [ ] 创建 `src/components/FormRenderer.tsx`
- [ ] 实现基础的组件渲染逻辑
- [ ] 支持 Input 组件的渲染

### 4. 验证数据驱动逻辑

- [ ] 在页面左侧放一个"添加 Input"按钮
- [ ] 点击按钮后，往 Store 里的 items 数组 push 一个 Input 配置对象
- [ ] 确保右侧画布能自动多出一个输入框

---

## ⚠️ 重要提示

**不要急着装 dnd-kit！** 拖拽库配置复杂，先完成数据驱动的核心逻辑。

一旦这一步通了，说明"数据驱动"的架构是可行的，后续集成拖拽会更顺利。

---

## 📊 完成标准

- ✅ 依赖安装成功
- ✅ Schema 类型定义完整
- ✅ Renderer 能正确渲染 Input 组件
- ✅ 点击按钮后，画布能动态增加输入框

---

## 📝 备注

- 保持代码简洁，避免过度设计
- 优先验证核心逻辑，细节优化可后续迭代
- 遇到问题时，先检查 Store 中的数据是否正确更新
