# React-Flow-Form 开发计划

## 第一阶段：协议定义与类型系统 (The Schema)

这是项目的"骨架"。在动手写 UI 之前，必须先定义好 JSON 长什么样。

### 任务清单

- [ ] 新建 `src/types/schema.ts`
    - [ ] 定义 `ComponentType`（组件类型枚举）
    - [ ] 定义 `FormItemSchema`（单个组件的属性，如 id, type, label, props 等）
    - [ ] 定义 `PageSchema`（整个页面的结构）

- [ ] 定义物料配置 `src/config/materials.ts`
    - [ ] 分类基础类组件（Input, Select）
    - [ ] 分类布局类组件（Grid, Card）

---

## 第二阶段：状态管理与渲染引擎 (Store & Renderer)

在没有拖拽功能时，先实现"数据驱动页面"。

### 任务清单

- [ ] 搭建 Zustand Store
    - [ ] 实现 schema 的存储
    - [ ] 编写 `addComponent`（添加组件）Action
    - [ ] 编写 `updateProps`（修改属性）Action

- [ ] 实现渲染器 `FormRenderer`
    - [ ] 编写根据 JSON 自动生成 AntD 组件的逻辑
    - [ ] 验证：手动在 Store 里写死一段 JSON，看页面能否正确渲染出表单

---

## 第三阶段：布局与拖拽实现 (Editor Layout & Dnd)

这时候再让页面"动"起来。

### 任务清单

- [ ] 实现三段式布局
    - [ ] 左侧：物料列表
    - [ ] 中间：画布（Canvas），作为 dnd-kit 的 Drop 区域
    - [ ] 右侧：配置面板（Settings），点击画布组件时显示对应属性

- [ ] 引入 dnd-kit
    - [ ] 实现从左侧物料区拖拽到画布
    - [ ] 实现画布内的排序（Sortable）

---

## 第四阶段：配置面板与数据闭环 (Settings & Export)

让非技术人员也能改参数。

### 任务清单

- [ ] 动态属性表单
    - [ ] 点击 Input，右侧显示"修改占位符"
    - [ ] 点击 Select，右侧显示"配置选项"
    - [ ] 根据当前选中组件的 type 动态切换属性表单

- [ ] 导出功能
    - [ ] 实现"导出 JSON"功能
    - [ ] 实现"导出预览链接"功能

---

## 优先级说明

| 阶段     | 优先级 | 预计工期 |
| -------- | ------ | -------- |
| 第一阶段 | 🔴 高  | 1-2 天   |
| 第二阶段 | 🔴 高  | 2-3 天   |
| 第三阶段 | 🟡 中  | 3-5 天   |
| 第四阶段 | 🟡 中  | 2-3 天   |

---

## 技术要点

- **Schema 设计**：确保 JSON 结构能够完整描述表单
- **Store 设计**：使用 Zustand 的 immer 中间件处理嵌套状态更新
- **渲染逻辑**：递归处理嵌套布局
- **拖拽体验**：使用 dnd-kit 的 Sortable 预设实现流畅的排序
- **属性配置**：动态表单的关键是根据 ComponentType 映射不同的配置项

---

## 注意事项

- 每个阶段完成后进行集成测试
- 保持类型定义的完整性，避免后期重构
- 考虑性能优化，特别是大表单场景下的渲染效率
